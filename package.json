{
  "name": "skiddyskidskid",
  "version": "1.0.0",
  "main": "server.js", // or app.js if that's your entry point
  "scripts": {
    "start": "node server.js" // change to app.js if you use that
  },
  "dependencies": {
    "express": "^your-version",
    "request": "^your-version"
  }
}
